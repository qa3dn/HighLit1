// import { useProjects } from '../useProjects';

const Projects = () => {
  // const { data, isLoading } = useProjects();

  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold">Projects Content Coming Soon</h1>
    </div>
  );
};

export default Projects;
